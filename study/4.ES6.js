const hello = "hello";
// hello = "hi";    // -> 변경 불가(const)
console.log(hello);

const name = "KKK";

const plus = hello + ", " + name;
console.log(plus);

console.log(`${hello}! ${name} 백틱신기하지?`);
